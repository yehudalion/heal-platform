# מנת ייצור א' — lecture_qa (LQA-PROD-A), 14.8.2026

**סטטוס: נכתבה ל-DB, `is_published=false` על הכל. ממתינה לביקורת ליאון.**
נבנתה אוטונומית לפי הנחיית ליאון ("לבנות את כל הפינה מקצה לקצה"), לפי
`LECTURE_QA_AUDIT_PROTOCOL.md` v2 ו-`LISTENING_FORMAT.md` v2.3.

## הרכב המנה

| מפתח | id | דלי | פורמט | צורת קטע | שאלות | סוגי שאלה |
|---|---|---|---|---|---|---|
| A1 | `76e50c64` | s30 | lecture | תיאורי-מצטבר | 1 | main_idea (תיאורי) |
| A2 | `ea531be8` | s30 | dialogue | שרשרת-הסבר | 1 | detail |
| A3 | `78afc85c` | s60 | lecture | פנייה-אחת | 2 | main_idea (פנייתי) · **vocab_in_context** |
| A4 | `1a7976e3` | s60 | dialogue | מדריך+מבקר | 2 | speaker_ref · **negative** |
| A5 | `cb759760` | s90 | lecture | שתי-פניות+מונחים | 2 | main_idea (פנייתי) · detail |
| A6 | `243ec943` | s90 | dialogue | שרשרת-הסבר | 2 | detail · **opinion** |

מכסה את `vocab_in_context` שחסר בכיול (מטרת מנה א' לפי ה-roadmap).
סוגים חדשים: 3/10 = 30% (יעד ~25%). שני הצורות (טיעון/תיאורי) קיימות ✓.
שמות הדוברים (Maya/Tom/Daniel/Priya) נבחרו מתוך `KNOWN_GENDER` של סקריפט
הרינדור; Guide/Visitor מקבלים fallback תקין (זכר/נקבה).

## שערים מכניים — כולם עברו

| שער | תוצאה |
|---|---|
| מילים לקטע | A1:93 · A2:95 · A3:158 · A4:149 · A5:235 · A6:219 — כולם בטווח v2.3 (שניים תוקנו: A3 היה 136, A6 היה 209) |
| שאלות לקטע | s30→1, s60→2, s90→2 ✓ |
| פיזור תשובות | A:3 B:3 C:2 D:2 ✓ |
| נכונה-הארוכה | 2/10 = 20% (תקרה 30%) ✓ |
| פער אורך | אין אופציה קצרה ב-≥6 מילים מהממוצע ✓ |
| unsupported | 2/30 = 6.7% (תקרה 15%) ✓ |
| מגוון k_code | 7 סוגים ✓ |
| התאמת אוצר fail_mode | לפי טבלת v2.3 לכל סוג שאלה ✓ |
| איות | אמריקאי (color/center), אפס בריטי ✓ |
| מספרים | במילים בלבד ✓ |
| highlight_spans | 2–3 לקטע, ≤6 מילים, מילה-במילה, ללא חפיפה ✓ |
| הסברים | 40/40, ≤250 תווים, ציטוטים מילה-במילה ✓ |

## פותר עיוור דו-כיווני (שני סוכנים נקיים)

**פתירות: 10/10** — כל שאלה נפתרת מהתמליל לתשובה המיועדת בביטחון גבוה.
הסתייגות אחת רכה (A2: "סיבת שורש מול סיבה ישירה") — הפותר בכל זאת בחר נכון
בביטחון, בזכות ניסוח השאלה "לפי טום, למה **הוחלפו**".

**דליפה: 8/10 בביטחון.** אותה תבנית שנחשפה במנת הכיול ותועדה שם:
- main_idea — הנכונה היא תמיד המקיפה-המסכמת (מבני לסוג השאלה; קיים גם במבחן האמיתי).
- A5-Q2 נענית מידע-עולם (קשתות דוחפות הצידה) — מסיחים נאמנים לא מתקנים את זה.
- **תוקן בפועל:** ב-A4 (שאלת NOT) הנכונה המקורית הייתה אבסורדית פיזיקלית
  ("לשפוך דגן להאטה") — הוחלפה באמצעי סביר-אך-לא-נאמר ("קשירת הכנפיים בחבלים",
  שהוא אמצעי אמיתי היסטורית ולכן מפתה).
כמו במנת הכיול: הדליפה השיטתית מדווחת ולא נרדפת לאפס — תיקונה מחייב
החלשת נכונות או שבירת מנגנונים מתועדים.

## מה נשאר לליאון

1. ביקורת תוכן (זה מה ש-PENDING LION REVIEW ב-source_context אומר).
2. רינדור אודיו: `node scripts/generate-lecture-qa-audio.js --all` (ירונדרו רק
   6 החדשים — לישנים כבר יש audio_url; המנה תקבל קולות עוקבים מהמאגר לפי rank).
3. QA: `python3 scripts/qa_listening_audio.py scripts/audio_output_lecture_qa --type lecture_qa --strict`
4. פרסום (`is_published=true`) — רק אחרי 1–3.
